import { type ProjectGetAllParametersDto } from '~/packages/projects/projects.js';

const DEFAULT_PROJECT_FILTER_PAYLOAD: ProjectGetAllParametersDto = {
  search: '',
};

export { DEFAULT_PROJECT_FILTER_PAYLOAD };
